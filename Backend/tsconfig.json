{
  "compilerOptions": {
    "target": "es2021", // Define a versão do ECMAScript de destino
    "module": "commonjs", // Define o sistema de módulos
    "outDir": "./dist", // Diretório de saída para os arquivos compilados
    //"rootDir": "./src", // Diretório raiz dos seus arquivos TypeScript
    "strict": true, // Habilita uma série de checagens de tipo rigorosas
    "esModuleInterop": true, // Permite a interoperabilidade entre módulos CommonJS e ES
    "skipLibCheck": true, // Pula a verificação de tipos de arquivos de declaração
    "forceConsistentCasingInFileNames": true, // Garante que os nomes de arquivos sejam consistentes em termos de maiúsculas e minúsculas
    "types": ["jest", "node"]
  },
  "include": ["src/**/*", "tests/**/*"], // Inclui todos os arquivos dentro da pasta 'src' para compilação
  "exclude": ["node_modules"] // Exclui a pasta 'node_modules' da compilação
}